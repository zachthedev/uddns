{
  "name": "uddns",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.2",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "bun scripts/deploy.ts",
    "cf-typegen": "wrangler types --env-file .dev.vars.template",
    "cf-typegen:check": "rm -f worker-configuration.d.ts && bun run cf-typegen && git diff --no-ext-diff --exit-code -- worker-configuration.d.ts",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest watch",
    "lint": "bunx --bun --no-install eslint --config eslint.config.ts . --max-warnings=0",
    "lint:fix": "bunx --bun --no-install eslint --config eslint.config.ts . --fix",
    "format": "bunx --bun --no-install prettier --config .prettierrc --no-editorconfig --ignore-path .prettierignore --write .",
    "format:check": "bunx --bun --no-install prettier --config .prettierrc --no-editorconfig --ignore-path .prettierignore --check .",
    "typecheck": "bun node_modules/@typescript/native/bin/tsc --noEmit -p tsconfig.json && bun node_modules/@typescript/native/bin/tsc --noEmit -p tests/tsconfig.json && bun node_modules/@typescript/native/bin/tsc --noEmit -p scripts/tsconfig.json",
    "audit": "bun audit --audit-level=high",
    "check": "bun scripts/check.ts",
    "check:quick": "bun scripts/check.ts --quick",
    "check:rows": "bun scripts/check.ts --rows",
    "prepare": "bunx --bun --no-install lefthook install"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.1.13",
    "@commitlint/cli": "21.2.2",
    "@commitlint/config-conventional": "21.2.2",
    "@eslint/js": "10.0.1",
    "@types/bun": "1.4.2",
    "@types/node": "26.6.2",
    "@typescript/native": "npm:typescript@7.0.2",
    "@vitest/coverage-istanbul": "4.1.11",
    "eslint": "10.11.0",
    "eslint-config-prettier": "10.1.8",
    "jiti": "2.7.0",
    "lefthook": "2.1.14",
    "prettier": "3.9.8",
    "typescript": "6.0.3",
    "typescript-eslint": "8.70.0",
    "vitest": "4.1.11",
    "wrangler": "4.135.0",
    "zod": "4.6.5"
  },
  "dependencies": {
    "cloudflare": "7.1.0"
  }
}
