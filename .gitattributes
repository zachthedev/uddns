# One line ending on every host, whatever a contributor's core.autocrlf says: every hook and script runs
# on every host, and Prettier's endOfLine default is lf, so a CRLF checkout fails the format check.
* text=auto eol=lf
