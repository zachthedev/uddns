# Line endings are the same on every machine, whatever a contributor's core.autocrlf says. The gate's
# format check requires LF (`endOfLine` in .prettierrc), so a CRLF checkout fails it on every file.
* text=auto eol=lf
