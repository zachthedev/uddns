# Bun waits three days before resolving a newly published version, which is the
# window a malicious release is usually pulled in. The updater's lock file
# maintenance and CI run `bun install` in containers that carry no other
# configuration, so this file is the one cooldown those runs observe.
[install]
minimumReleaseAge = 259200 # 3 days
